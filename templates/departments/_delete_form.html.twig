<form method="post" action="{{ path('departments_delete', {'idDepartment': department.idDepartment}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ department.idDepartment) }}">
    <button class="btn">Delete</button>
</form>
