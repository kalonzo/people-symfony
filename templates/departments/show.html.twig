{% extends 'base.html.twig' %}

{% block title %}Departments{% endblock %}

{% block body %}
    <h1>Departments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdDepartment</th>
                <td>{{ department.idDepartment }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ department.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('departments_index') }}">back to list</a>

    <a href="{{ path('departments_edit', {'idDepartment': department.idDepartment}) }}">edit</a>

    {{ include('departments/_delete_form.html.twig') }}
{% endblock %}
