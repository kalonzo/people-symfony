<form method="post" action="{{ path('unemployment_funds_delete', {'idUnemploymentFund': unemployment_fund.idUnemploymentFund}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unemployment_fund.idUnemploymentFund) }}">
    <button class="btn">Delete</button>
</form>
