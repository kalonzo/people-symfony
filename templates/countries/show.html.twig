{% extends 'base.html.twig' %}

{% block title %}Countries{% endblock %}

{% block body %}
    <h1>Countries</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCountry</th>
                <td>{{ country.idCountry }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ country.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('countries_index') }}">back to list</a>

    <a href="{{ path('countries_edit', {'idCountry': country.idCountry}) }}">edit</a>

    {{ include('countries/_delete_form.html.twig') }}
{% endblock %}
